#include <Windows.h>

LRESULT WinProc(HWND hWnd, UINT uMsg, WPARAM wParam, LPARAM lParam)
int WINAPI WinMain(HINSTANCE hInstance, HINSTANCE hPrevInstanc, PSTR lpCmdLine, int nCmdShow)
{
    WNDCLASSA class = {
        0,
    }
    return 0;
}
